# Ablation: 16x16 -> 32x32 scene memory.
# Everything else inherits from configs/config_v1.yaml (only mem_res and the
# checkpoint dir change).
base: configs/config_v1.yaml

model:
  global_mem_res: 32        # 32x32 = 1024 map tokens (vs 16x16 = 256)

train:
  ckpt_dir: "outputs/ckpt_v1_mem32"

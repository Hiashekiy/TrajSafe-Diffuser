# Ablation: 16x16 -> 8x8 scene memory (coarser global view).
# Everything else inherits from configs/config_v1.yaml (only mem_res and the
# checkpoint dir change).
base: configs/config_v1.yaml

model:
  global_mem_res: 8         # 8x8 = 64 map tokens

train:
  ckpt_dir: "outputs/ckpt_v1_mem8"
